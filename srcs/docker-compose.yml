version: '3'

services:
#wordpress and php-fpm
  mariadb:
    container_name: mariadb
    build: ./requirements/mariadb/
    restart: always
    env_file:
      - ./.env
    volumes:
      - database:/var/lib/mysql
      # - /home/colonel/data/vol_db/wordpress:/var/lib/mysql/wordpress
  wordpress:
    depends_on:
      - mariadb
    container_name: wordpress
    build: ./requirements/wordpress/
    restart: always
    env_file:
      - ./.env
    volumes:
      # - wordpress:/var/hjrifi.ma
      - /home/colonel/data/vol_wp/wordpress:/var/wordpress
  nginx:
    depends_on:
      - wordpress
    container_name: nginx
    build: ./requirements/nginx/
    restart: always
    ports:
      - "443:443"
    volumes:
      # - wordpress:/var/hjrifi.ma
      - /home/colonel/data/vol_wp/wordpress:/var/wordpress
  
volumes:
  database:
  # wordpress: